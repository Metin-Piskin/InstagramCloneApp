import { StyleSheet } from "react-native";

export default StyleSheet.create({
    button: {
        justifyContent: "center",
        alignItems: "center",
        height: 120,
        width: 150,
        opacity: 0.5,
    },
});